# Enlarge the macro behavior queue to comfortably accommodate multi-key macros
# (tmux splits, vim_wq, =>/->).  Default is 64.
CONFIG_ZMK_BEHAVIORS_QUEUE_SIZE=256

# Longer idle timeout before the keyboard goes to sleep (ms).
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000


